<%= form_for @foal, method: :put do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :img_url %>
<%= f.text_field :img_url %>

<%= f.label :broodmare_id %>
<%= f.text_field :broodmare_id %>

<%= f.submit %>

<%  end %>

<%= form_for @foal, method: :delete do |f| %>
  <%= f.submit "Delete" %>
<% end %>
