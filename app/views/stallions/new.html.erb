<h1>Arise for the New Stallion!</h1>

<%= form_for @stallion do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :img_url %>
<%= f.text_field :img_url %>

<%= f.label :stallion_id %>
<%= f.text_field :stallion_id %>

<%= f.submit "Submit" %>
<% end %>
